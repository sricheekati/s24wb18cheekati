extends layout

block content
  h1= title
  p Welcome to #{title}
  p This is a paragraph.
  button(type="button", class="btn btn-secondary") I am button
  br 
  br
  button(onclick="double()", class="btn btn-secondary") Double 

  div(id="double") Up the amount

  script.
    let value = 0;
    let power = 1;

    function double() {
      value++;
      power *= 2;
    
      document.getElementById("double").innerHTML = 
        `Two to the power ${power} is ${value}`;
   }